name: easy_permission
description: Flutter 权限插件
version: 1.0.0
author: xiao
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  plugin:
    platforms:
      android:
        package: cn.jjvu.xiao.easy_permission
        pluginClass: EasyPermissionPlugin
      ios:
        pluginClass: EasyPermissionPlugin